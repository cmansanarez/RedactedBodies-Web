<!DOCTYPE html>
<html>
    <%- include('head', { title: 'Archive | Noir Mak', description: 'The Archive — a complete record of projects by Noir Mak spanning generative art, live coding, interactive installations, and web-based work.' }) %>
    <body>

        <%- include('header') %>

        <main>

            <!-- PROJECTS BANNER -->
            <section id="projects-banner">
                <div class="container">
                    <p class="featured-label">Projects</p>
                    <h1 class="hero-title">The Archive</h1>
                </div>
            </section>

            <!-- PROJECTS GRID -->
            <section id="projects-grid">
                <div class="container">
                    <% const accents = ['card-accent-1', 'card-accent-2', 'card-accent-3', 'card-accent-4']; %>
                    <div class="row g-4">
                        <% projectArray.forEach((item, index) => { %>
                            <div class="col-lg-4 col-md-6">
                                <div class="card project-card <%= accents[index % 4] %>">
                                    <img src="/images/<%= item.image %>" class="card-img" alt="<%= item.name %>">
                                    <div class="card-img-overlay d-flex align-items-end">
                                        <h5 class="project-title">
                                            <a href="/project/<%= index + 1 %>" class="stretched-link"><%= item.name %></a>
                                        </h5>
                                    </div>
                                </div>
                            </div>
                        <% }) %>
                    </div>
                </div>
            </section>

        </main>

        <%- include('footer') %>

    </body>
</html>
